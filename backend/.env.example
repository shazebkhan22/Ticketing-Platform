PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://cygnus:cygnus_dev_password@localhost:5432/cygnus_ticketing
SESSION_SECRET=change_this_to_a_long_random_string
SESSION_MAX_AGE_MINUTES=30
FRONTEND_ORIGIN=http://localhost:5173

# Individual user accounts are defined directly in src/db/seed.ts (SEED_USERS array),
# not via env vars, since each of the 5 employees has their own username/password.
